# Background Images

Battle backgrounds live here.

- Current dungeon background: `dungeon-bg.png`

To replace it, overwrite `dungeon-bg.png` or edit the `.enemy-art` background URL in `frontend/src/styles.css`.
